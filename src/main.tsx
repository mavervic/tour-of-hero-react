import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import QuickStartApp from './quick-start/QuickStartApp.tsx';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    {/* <App /> */}
    <QuickStartApp />
  </React.StrictMode>
);
